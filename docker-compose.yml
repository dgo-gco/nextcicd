services:
  node:
    container_name: node
    image: node:26-alpine3.24
    working_dir: /front
    volumes: 
      - ./:/front
    ports:
      - "3000:3000"
    tty: true
    networks:
      - myapp

  postgres:
    container_name: postgres
    image: postgres:15.4-alpine3.24
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: nextcicd
    # ports:
    #   - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/18/docker
    networks:
      - myapp

  redis:
    container_name: redis
    image: redis:8.8.0-alpine3.23
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    environment:
      - REDIS_ARGS="--appendonly yes"
    networks:
      - myapp

networks:
  myapp:
    driver: bridge

volumes:
  pgdata:
  redis-data:
